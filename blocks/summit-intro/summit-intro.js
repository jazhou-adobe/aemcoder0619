export default async function decorate(block) {
  // The block is straightforward centered text sections.
  // EDS will auto-decorate the strong-wrapped links as buttons.
  // No additional DOM restructuring needed.
}
